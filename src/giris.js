




const giris = () =>{
	return(
		<div>
			<p> Giriş ekranı</p>
			{/* <button onClick={()=>{
				if(account) return;
					connect()
				}}> {account ? "Connected" : "Connect"} 
	  		</button> */}
		</div>
	)
	
}

export default giris